---
description: Save the current session as a handoff doc so the next session continues cleanly.
---

Write a handoff for the current session of Tasker.

Follow `.claude/skills/handoff`:
1. Create `docs/sessions/<YYYY-MM-DD-slug>.md` with: Done, Current state (branch /
   merged / tests), the single Next action, Open questions/risks, Key files.
2. Append a one-line entry to `MEMORY.md`'s session index (date — topic — link).
3. Keep it terse — a pointer, not a transcript.

Report the handoff file path.
