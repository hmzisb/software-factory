---
description: Plan only — decompose a request into vertical-slice tickets with dependency waves. No implementation.
argument-hint: "<feature request>"
---

Plan, do not implement: **$ARGUMENTS**

Dispatch the `planner` agent (`.claude/agents/planner.md`). It reads
`CONTEXT.md` + relevant code and writes vertical-slice tickets to `plans/` with
dependency waves, acceptance criteria, file hints, and a test plan per ticket.

Then summarize the plan: the waves, the tickets, and any open questions. Stop —
do not start building. The user runs `/factory-build` (or approves) to proceed.
