# Patterns ledger

Maintained by the `documentator`. Each entry is a recurring friction observed
across reviews, repeated fixes, and gotchas — captured so the factory stops
repeating them.

Entries are advisory: humans review them to decide what should become a rule, a
hook, or a CONTEXT.md change.

<!-- Patterns appear below this line. Preserve this header verbatim. -->
