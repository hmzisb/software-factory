name: factory-ci

on:
  pull_request:
  push:
    branches: [main, master]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Toolchain + dependencies for {{ stack.language }} ({{ stack.package_manager }}):
{{ computed.ci_setup }}

      # Validation gates (from quality.* in factory.config.json):
{{ computed.ci_steps }}
